import { StyleSheet } from "react-native";

export const InputStyles = StyleSheet.create({
    text: {
        width: "100%",
        height: 45,
        borderWidth: 0.5,
        borderRadius: 8,
        backgroundColor: "#fff",
    }
})